<div class="row">
  {{#post}}
  <div class="col-sm-8 col-sm-offset-2">
    <li class="list-group-item">
      <a href="/posts/{{this._id}}" class="lead">{{this.title}}</a>
    </br>
    <p>Submitted by {{this.author.username}}<p>
      <a href="{{this.videoUrlurl}}" target="_blank">{{this.videoUrlurl}}<a>
      </div>
    </li>
  </div>

    {{/post}}
</div>
